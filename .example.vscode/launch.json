{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch",
      "program": "${workspaceFolder}/bin/ifjcompiler_debug",
      "stdio": "${workspaceFolder}/tests/test.in",
      "cwd": "${workspaceFolder}",
      // "preLaunchTask": "Build and Clear",
      "console": "integratedTerminal"
    }
  ]
}
