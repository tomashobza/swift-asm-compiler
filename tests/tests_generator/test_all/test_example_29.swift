    var x = 10
    var y = 20
    var sum = x + y

    if sum > 15 { 
        var message = 'Sum is ' + sum
        if sum < 30 { 
            message = message + ', which is less than 30' }
            write(message)
}